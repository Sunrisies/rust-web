pub fn create_images() -> Vec<String> {
    let mut images: Vec<String> = Vec::new();
    images.push("https://via.placeholder.com/150".to_string());
    images.push("https://via.placeholder.com/200".to_string());
}
